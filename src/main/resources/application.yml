spring:
  application:
    name: forum-hub
  datasource:
    url: jdbc:mysql://localhost:3306/db_forumhub_dev
    username: user
    password: devdatabase1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
